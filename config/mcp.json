{
  "mcpServers": {
    "zabbix-readonly": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/path/to/zabbix-mcp-server",
        "zabbix-mcp-readonly"
      ],
      "env": {
        "ZABBIX_URL": "http://localhost/zabbix",
        "ZABBIX_TOKEN": "your-token-here"
      }
    },
    "zabbix-writable": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "/path/to/zabbix-mcp-server",
        "zabbix-mcp-writable"
      ],
      "env": {
        "ZABBIX_URL": "http://localhost/zabbix",
        "ZABBIX_TOKEN": "your-token-here",
        "READ_ONLY": "false"
      }
    }
  }
}
